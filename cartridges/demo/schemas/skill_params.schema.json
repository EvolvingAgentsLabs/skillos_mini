{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "SkillParams",
  "description": "Parameters extracted by the LLM for a Gallery JS skill invocation.",
  "type": "object",
  "properties": {
    "skill_name": {
      "type": "string",
      "description": "Name of the Gallery skill to execute (e.g., 'calculate-hash')."
    },
    "data": {
      "type": "string",
      "description": "JSON string with skill-specific parameters (e.g., '{\"text\": \"hello\"}')."
    },
    "secret": {
      "type": "string",
      "description": "Optional API key or secret for skills that require one.",
      "default": ""
    }
  },
  "required": ["skill_name", "data"],
  "additionalProperties": false
}
