<?xml version="1.0" encoding="utf-8"?>
<!--
  Copy to android/app/src/main/res/xml/network_security_config.xml after
  `npx cap add android`. Then reference it from the manifest:

    <application
        android:networkSecurityConfig="@xml/network_security_config"
        android:usesCleartextTraffic="true"
        ... >

  Permits cleartext HTTP only to RFC-1918 private ranges (home-LAN Ollama).
  Public-internet traffic remains HTTPS-only.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="true">localhost</domain>
        <domain includeSubdomains="true">10.0.0.0/8</domain>
        <domain includeSubdomains="true">172.16.0.0/12</domain>
        <domain includeSubdomains="true">192.168.0.0/16</domain>
    </domain-config>
    <base-config cleartextTrafficPermitted="false">
        <trust-anchors>
            <certificates src="system" />
        </trust-anchors>
    </base-config>
</network-security-config>
